<div class='row'>
  <div class='col-xs-12'>
    <div class='card card-block'>
      <h4>Event Controls</h4>
      <%= link_to 'Manage Event', event_attributes_path(event), class: 'btn btn-square btn-primary' %>
      <div class='float-xs-right'>
        <%= link_to 'Edit Event', edit_event_path(event), class: 'btn btn-square btn-warning' %>
        <%= link_to 'Delete Delete', edit_event_path(event), class: 'btn btn-square btn-danger' %>
      </div>
    </div>
  </div>
</div>